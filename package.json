{
  "name": "ventas-express-license-api",
  "version": "1.0.0",
  "private": true,
  "description": "API + panel admin para licencias revocadas (Vercel + Upstash Redis gratis)",
  "dependencies": {
    "@upstash/redis": "^1.34.3"
  }
}
